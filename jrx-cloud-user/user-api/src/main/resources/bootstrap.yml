spring:
  profiles:
    active: dev
  cloud:
    config:
      discovery:
        serviceId: jrx-cloud-config
        enabled: true
      name: user-api, mybatis
      label: master
      profile: ${spring.profiles.active}